{
  "name": "biraderagentic",
  "version": "1.0.0",
  "description": "BiraderAgentic — Vergi Merkezi portal MCP Worker (Cloudflare)",
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('biraderagentic: bundle yok; Cloudflare Workers src/index.js dogrudan kullanir.')\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
