{
  "name": "base-mcp-server",
  "version": "1.0.0",
  "description": "Vergi Merkezi BASE Ön Muhasebe — MCP Server (Cloudflare Workers)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
